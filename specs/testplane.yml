feature: Поддержка testplane
code: testplane
description: Обогащение модели данными из отчета testplane, сформированного плагином json-reporter

specs-unit:
  Идентификатор ФТ:
    - assert: Идентификатор ФТ собирается из перечисленных в keys сегментов, в указанном порядке

    - assert: Для сегмента, заданного через символ собаки, подставляется код значения атрибута фичи

    - assert: Для сегмента, заданного через символ доллара, подставляется название значения атрибута фичи

    - assert: Если у фичи несколько значений атрибута, указанного в keys, тест с ней не сопоставляется

    - assert: Если у фичи не проставлен атрибут, указанный в keys, тест с ней не сопоставляется

  Разбор отчета:
    - assert: Полное имя теста собирается из suitePath

    - assert: Тест со статусом success или fail делает ФТ автоматизированным

    - assert: Тест со статусом error или skipped помечает ФТ как проблемный

  Сопоставленные тесты:
    - assert: Тест, полное имя которого совпало с идентификатором ФТ, добавляется в модель как сопоставленный с этим ФТ

    - assert: Запуски одного теста в разных браузерах дают ФТ один сопоставленный тест
      description: В отчете такой тест занимает по записи на каждый браузер; записи отличаются полем browserId, но ссылаются на один файл

    - assert: Тесты с одинаковым suitePath из разных файлов дают ФТ два сопоставленных теста

    - assert: Если идентификаторы двух ФТ совпадают, тест сопоставляется с обоими

    - assert: Тест, не сопоставленный ни с одним ФТ, попадает в отчет как тест без описания
